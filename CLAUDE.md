# Project instructions

- After making changes, always ask before committing to git. Never run `git commit` (or `git push`) without the user explicitly confirming first, even if a commit message was already prepared/approved in conversation.
